<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Sistema de Gerenciamento de Dados</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="fas fa-database"></i> Sistema de Gerenciamento
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="/"><i class="fas fa-home"></i> Dashboard</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle active" href="#" id="cadastrosDropdown" role="button" data-bs-toggle="dropdown">
              <i class="fas fa-plus-circle"></i> Cadastros
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item active" href="/cadastros/familias"><i class="fas fa-sitemap"></i> Famílias</a></li>
              <li><a class="dropdown-item" href="/cadastros/tamanhos"><i class="fas fa-expand"></i> Tamanhos</a></li>
              <li><a class="dropdown-item" href="/cadastros/cores"><i class="fas fa-palette"></i> Cores</a></li>
            </ul>
          </li>
        </ul>
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
              <i class="fas fa-user"></i> <%= typeof user !== 'undefined' ? user.email : 'Usuário' %>
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt"></i> Sair</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header">
            <h3><i class="fas fa-sitemap"></i> <%= title %></h3>
          </div>
          <div class="card-body">
            <form method="POST" action="<%= familia ? `/cadastros/familias/${familia.id}` : '/cadastros/familias' %>">
              <div class="row">
                <div class="col-md-6">
                  <div class="mb-3">
                    <label for="nome" class="form-label">Nome *</label>
                    <input type="text" class="form-control" id="nome" name="nome" value="<%= familia ? familia.nome : '' %>" required>
                  </div>
                </div>
                <div class="col-md-6">
                  <div class="mb-3">
                    <label for="codigo_old" class="form-label">Código Legado</label>
                    <input type="text" class="form-control" id="codigo_old" name="codigo_old" value="<%= familia ? familia.codigo_old || '' : '' %>">
                    <div class="form-text">Código do sistema legado para rastreabilidade</div>
                  </div>
                </div>
              </div>

              <div class="mb-3">
                <label for="nome_old" class="form-label">Nome Legado</label>
                <input type="text" class="form-control" id="nome_old" name="nome_old" value="<%= familia ? familia.nome_old || '' : '' %>">
                <div class="form-text">Nome antigo do sistema legado</div>
              </div>

              <div class="mb-3">
                <label for="legado" class="form-label">Campo Legado</label>
                <input type="text" class="form-control" id="legado" name="legado" value="<%= familia ? familia.legado || '' : '' %>">
                <div class="form-text">Concatenação de código e nome dos padrões antigos</div>
              </div>

              <div class="mb-3">
                <label for="descricao" class="form-label">Descrição</label>
                <textarea class="form-control" id="descricao" name="descricao" rows="3"><%= familia ? familia.descricao || '' : '' %></textarea>
              </div>

              <% if (familia) { %>
                <div class="mb-3 form-check">
                  <input type="checkbox" class="form-check-input" id="ativo" name="ativo" value="1" <%= familia.ativo ? 'checked' : '' %>>
                  <label class="form-check-label" for="ativo">Ativo</label>
                </div>
              <% } %>

              <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                  <i class="fas fa-save"></i> <%= familia ? 'Atualizar' : 'Salvar' %>
                </button>
                <a href="/cadastros/familias" class="btn btn-secondary">
                  <i class="fas fa-arrow-left"></i> Voltar
                </a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>